# Dev : le frontend (Vite, 5173) appelle le backend Express local (3001).
VITE_API_URL=http://localhost:3001

# Token Mapbox (carte interactive). Le vrai token va dans .env.development.local
# (non suivi par git). Ne jamais committer de token ici.
VITE_MAPBOX_TOKEN=
