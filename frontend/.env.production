# Prod : front et back sur le même domaine → URL d'API relative (zéro CORS).
VITE_API_URL=

# Token Mapbox : injecté au build via la variable d'env Render VITE_MAPBOX_TOKEN
# (on ne la déclare pas vide ici pour ne pas écraser la valeur fournie par Render).
